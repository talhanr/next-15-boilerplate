export enum UserCommonKeys {
  USER = "user",
  IS_LOADING = "isLoading",
}
export enum UserKeys {
  ID = "id",
  USER_NAME = "userName",
  EMAIL = "email",
  FIRST_NAME = "firstName",
  LAST_NAME = "lastName",
  ROLE = "role",
}
